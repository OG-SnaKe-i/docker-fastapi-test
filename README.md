# docker-fastapi-test

## Run with Docker

```bash
# Build and start
docker compose up --build -d

# Stop containers
docker compose down

# Restart(Data Persistent)
docker compose up -d


👉 http://localhost:8000/docs
